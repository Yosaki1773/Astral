import { describe, it, expect } from "vitest";

describe("urlUtil", () => {
    it("has no exports anymore (convertOpenaiToResponses was removed)", () => {
        // 占位：convertOpenaiToResponses 已随 sgVendor 协议判定口径调整而删除。
        // 保留此 describe 以便将来重新加 URL 工具时挂载在该文件下。
        expect(true).toBe(true);
    });
});